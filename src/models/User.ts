export interface User {
 idUser : number;
 identite: string;
 login: string;
 password: string;

 /*
 constructor(identite?: string, login ?: string, password ?: string, idUser ?: number, ){

   // this.idUser = idUser;
    this.identite = identite!;
    this.login = login!;
    this.password = password!;
 }

 */
}
